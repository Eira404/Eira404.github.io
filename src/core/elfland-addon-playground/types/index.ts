export interface PlaygroundFiles {
  html: {
    name: string
    code: string
  }
  css: {
    name: string
    code: string
  }
  ts: {
    name: string
    code: string
  }
  js: {
    name: string
    code: string
  }
}
